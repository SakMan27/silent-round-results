// config.js — deployment settings.
//
// The proxy Worker that fetches Calicotab pages server-side (see worker/).
// This is set once by the site owner; end users never see or touch it.
// To change proxies later, just edit this one line.
export const WORKER_URL = "https://silent-round-proxy.saket-manikantan.workers.dev";
